# Developing The Heart — Netlify configuration
#
# The site is a static folder with no build step. `publish` points at it
# directly; there is nothing to compile.

[build]
  publish = "site"
  command = ""
